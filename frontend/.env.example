# --- frontend → backend wiring ---
# Standalone API + auth server (../backend). Browsers stay same-origin:
# next.config.js rewrites /api/auth/* and /api/me to this URL.
BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000
